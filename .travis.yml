language: ruby
rvm:
  - "2.4.0"

branches:
  only:
    - master

before_script:
  - ruby -v

script:
  - cd ~/
  - git clone --depth 1 git@github.com:Hiroto-K/dotfiles.git ~/dotfiles
  - cd ~/dotfiles
  - bash setup.sh
  - bash tests/test.sh

notifications:
  on_success: never
  on_failure: never
